# VRishi Academy — Session Handoff (2026-08-09)

## Who
Jeeth (Jithendran Sellamuthu), C.MH., AHA #007913, HMI 720-hr IDL program
(Semester 2 / 501 internship). Building VRishi Academy: a Kappasinian
hypnotherapy practice drill + role-play platform, separate from the
client-facing VRishiHypno stack (compliance boundary). Terse mode. Zero
hallucination. Ground-truth-first. Test-before-deliver. Filesystem MCP to
`D:\ChatGPT Projects\genai-portfolio\projects\`.

Mentor: Denise Delahoussaye, MA, CCHt (PSR guide). Grad deadline: Dec 10 2026.

## Repo root
`D:\ChatGPT Projects\genai-portfolio\projects\Jeeth.ai\Business\VRishiHypno\PrepPractices\`

## What exists (ALL verified on his machine)

### Infrastructure & scripts
- `services/academy-compose.yml` — 6-service docker overlay on jeethhypno-shared-network,
  ports 8600-8605, external network, env anchor -> pgbouncer:6432 db `academy`.
- `services/.env` — generated from Jeeth.ai root .env: ACADEMY_DB_PASSWORD=POSTGRES_PASSWORD,
  MinIO, Redis (empty pw), LiveKit, Ollama models, ELEVENLABS_API_KEY= (blank -- key rotation pending).
- `services/Academy-Start-All-Services.ps1` — infra preflight (5 containers), backend
  docker-compose guard (partial Dockerfile detection), Next.js web launcher with port-kill
  + health-poll. Import-AcademyDotEnv loader wired in.
- `academy.ps1` — master CLI: setup, infra, db-init (idempotent role+db via docker exec psql),
  render, web, backend, all, test, gap (interactive gap.json updater + Dec-10 countdown),
  status, endpoints, stop, help. Same dotenv loader.
- Port governance: 123 occupied ports mapped; academy claims 8600-8605 + 3070.

### Academy Web (Next.js 14, :3070)
- `/plan/[slug]` — 9-page plan viewer (00-overview through 08-maintenance).
- `/dojo` — gap dials + Dec 10 countdown, `data/gap.json`.
- `/lab` — Practice Lab: **19 drills**, **7 sequences**, **9 presets**. Grounded in PSR Scripts
  workbook (items 1-18) + First Consultation workbook + NLP 1&2 workbooks (Tabbanella 201-2/201-3).
  Drills include: intake (Step 0a with LOVE, open->closed, well-formed outcome), settle, pretalk,
  tom, armraise (14-line prompter, 10-point check, all 4 nods, PHS anatomy), count50, reactional,
  heavylight, progrelax, staircase, suggestions, countout, fingerspread, homework, handforehead,
  armrigidity, eyefascination, nlp_rapport (7/38/55, match-mirror-pace-lead, sensory acuity),
  nlp_outcome (10 keys to achievable outcome).
  Sequences: psr_full (Denise's exam order), psr_core, first_session (14 drills),
  moreno_blueprint, kappas_vintage, var2_emotional, var2_physical.
  Presets: first55, psr15, tom5, full25, mock50, moreno34, kappas22, var2e30, var2p30.
  BuildPlan math property-tested EXACT across 3-90 min.

### Session-template engine (packages/session-templates/)
- `templates/_blocks/blocks.ssml.j2` (218 lines) — v1.2 woven blocks with:
  - Pre-talk: 3-truism yes-set -> lead ("and that means...")
  - ToM: tag-question yes-anchor (`await_tom_yes`) -- 16 nod checkpoints total
  - Conversion: authored `<emphasis level="strong">` on pivotal lightness + alternatives bind
    (fingers-or-wrist / hand-to-face), BOTH E and P modes
  - PR: VAK tri-weave ordered by `p.vak` profile field (visual/auditory/kinesthetic)
  - Suggestions: alternatives bind (tonight/tomorrow/unexpected) + embedded "trust yourself now"
  - Homework: future-pace next week's win
- `templates/session_first.ssml.j2`, `session_subsequent.ssml.j2`, 3 session plans (vocational/
  referral/avocational), 3 client profiles (p1 physical/visual, p2 emotional/kinesthetic, p3 child/default).
- `schema/axes.yaml` — age rates NOW ABSOLUTE (child 92%, teen 96%, adult 100%, elder 85%).
  Bug fixed: were relative "0%/-8%" = invalid SSML on Windows.
- `render/render_session.py` — Jinja2 renderer. All 4 IO sites explicit `encoding="utf-8"`.
  Auto-creates parent dirs (`mkdir(parents=True, exist_ok=True)`). Both fixed after live failures.
- `render/prosody.py` (228 lines) — SSML enrichment engine aligned to ssml_router.py/tts_service.py:
  - EP_VOICE_PARAMS (physical 95%/-5, emotional 85%/-2 pause x 1.3)
  - TONALITY_MODS (authority/paternal/maternal/conversational/theta_hypnotic)
  - STAGE_MAP: 15 stages -> zones/phases/tonalities; E-clients auto-swap challenge-adjacent to maternal
  - NLP: embedded commands -> `<emphasis level="strong">` + 350ms envelope -> CAPS in ElevenLabs;
    presuppositions -> moderate emphasis; `--vak` flag weaves client's rep-system predicates in therapy stages
  - `vak_counts` per segment in e11 plan meta
  - `ssml_to_elevenlabs()` vendored from ssml_router.py (breaks->dots, strong->CAPS)
  - E11 plan JSON: per-segment HypnoticTTSRequest bodies for POST to :8136
  - Contract: `e11-2026-08-08`
- `render/nlp_lexicon.yaml` — embedded commands, presuppositions, pace/lead, VAK predicates table
  (NLP 2), sensory acuity checklist (NLP 2), eye patterns (NLP 2).
- `PROSODY.md` — pipeline docs, v1.2 weave notes, security notes.
- Pipeline verified end-to-end: render -> enrich -> ElevenLabs API (HTTP 200, rachel voice, mp3).

### P0 slice 1 services (E2E TESTED, ENRICHMENT VERIFIED)
- `services/academy-orchestrator/` — main.py (v0.2.0), Dockerfile, requirements.txt.
  FastAPI; POST /sessions -> renders session via subprocess -> parse_turns() splits on
  stage_/await_/cue_ marks; WS /ws/{sid}: client sends {"cmd":"next"}, server pushes turn events;
  at await -> calls persona-svc /respond (httpx 8s, offline fallback "*nods*") -> pushes reply.
  **Enrichment**: every `line` turn carries `prosody` dict (rate, pitch, volume, tonality, pause_mult,
  zone, phase, pace) and optional `nlp` array (embed/presup/vak/lead/pace spans). Every `stage` turn
  carries `tonality`, `zone`, `phase`. Enrich=True by default.
  **TTS proxy**: POST /tts proxies to :8136, GET /tts/health checks TTS availability. Returns 503
  gracefully when TTS is offline.
- `services/persona-svc/` — main.py, Dockerfile, requirements.txt, personas/ (maya + leo cards).
  POST /respond -> tries Ollama /api/chat (gemma3:4b, 6s, 60 tokens), else deterministic fallback
  table per await checkpoint (all 16 named). Constraint C3: fictional archetypes only.
- Compose: build context = repo root (`context: ..`, `dockerfile: services/<svc>/Dockerfile`).

### DONE (this session)
1. **E2E WS test**: `test_e2e_ws.py` (in-process) + `test_enrichment_live.py` (against running services).
   All 3 profile/plan combos pass: p1/vocational (194t/16a/15s), p2/referral (176t/16a), p3/avocational (175t/16a).
   16/16 awaits get fallback replies, done event fires, all 153 lines have prosody, 39 have NLP marks,
   all 5 tonalities seen (authority/conversational/maternal/paternal/theta_hypnotic), 4 NLP types
   (embed/lead/presup/vak).
2. **`/studio` page**: Full WS role-play console with NLP highlight rendering (5 types color-coded),
   prosody badges (tonality icon + rate%), tonality chip per stage, NLP toggle, TTS audio button
   (hidden when :8136 offline). Nav link already wired.
3. **Prosody/NLP enrichment**: Inline in orchestrator (no subprocess for prosody.py). EP voice params,
   tonality mods, stage map (15 stages), NLP lexicon (16 embedded commands, 7 presuppositions, VAK
   predicates, pace/lead connectors). detect_nlp() with overlap dedup (longest match wins).
4. **TTS route fix**: prosody.py E11 plan endpoint corrected from `/api/v1/hypnotic-tts` to
   `/api/v1/tts/hypnotic/generate` (verified against tts_service.py route decorator).
5. **TTS proxy**: Orchestrator POST /tts proxies line turns to :8136 with correct HypnoticTTSRequest
   payload (text, zone, suggestibility_type, override_tonality, override_pace).

6. **db-init**: academy role + database created in jeethhypno-postgres (idempotent).
7. **Docker images built**: `vrishi-academy-academy-orchestrator` (275MB) and `vrishi-academy-persona-svc`
   (256MB). `docker compose -f academy-compose.yml --profile p0 build` passes. Profile split: p0 = built
   services (orchestrator + persona), p1 = placeholder services (asr/tts/grader/progress).
8. **NLP lexicon v2**: expanded from 4 types to 8 (embed/presup/vak/lead/pace + tag/bind/milton).
   Embedded commands 16→34, presuppositions 7→24, pace 4→12, lead 4→10. New: tag questions (10),
   double binds (8), Milton model (12). VAK predicates expanded with 20+ new terms. Full NLP 1/2
   workbook grounding: 13 presuppositions of NLP, 5 Principles, 10 Achievable Outcome keys, strategy
   steps, rapport model (7/38/55).
9. **Session blocks v1.2**: enhanced with NLP annotations — yes-sets (3 pace→1 lead), Milton model
   indirect patterns, double binds in conversion/suggestions, pacing current experience, presupposition
   language throughout, reframe in ToM, future-pace in homework. All original wording.

### NOT YET DONE
1. **`docker compose --profile p0 up -d`**: images built, but local services running on 8600/8601.
   Stop local → `docker compose -f academy-compose.yml --profile p0 up -d` to run containerized.
2. **asr-svc, tts-svc, grader-svc, progress-svc**: placeholder entries in compose (profile p1); no code.
3. **ElevenLabs key rotation**: live key is compromised (hardcoded at tts_service.py:482, byte-identical
   to AI_Pipeline_Code/.env). After rotation, paste new key into services/.env ELEVENLABS_API_KEY=.
4. **TTS audio playback**: Wire to ElevenLabs at :8136 when service is running. Studio UI has speaker
   buttons ready (hidden when TTS offline). Orchestrator proxy endpoint tested (returns 503 gracefully).
5. **NLP Speech Enrichment port drift**: registry 8416 vs source default 8417 vs docstring 8415.
8. **HMI gap sprint**: 24 contacts, 21 conferences, 78 elective hrs, 9 workshops before Dec 10 2026.

## Key files to read first (for the next chat)
```
PrepPractices/
  academy.ps1                            # master CLI
  services/
    .env                                 # all secrets
    academy-compose.yml                  # docker overlay
    Academy-Start-All-Services.ps1       # starter
    academy-orchestrator/
      main.py                            # orchestrator (read this)
      Dockerfile
    persona-svc/
      main.py                            # persona service (read this)
      Dockerfile
      personas/                          # maya + leo cards
  apps/academy-web/
    data/drills.json                     # 19 drills, 7 sequences, 9 presets
    data/gap.json                        # HMI progress tracker
    app/lab/page.jsx                     # Practice Lab UI
  packages/session-templates/
    templates/_blocks/blocks.ssml.j2     # v1.2 woven blocks (THE script)
    render/render_session.py             # Jinja2 renderer
    render/prosody.py                    # SSML+NLP enrichment
    render/nlp_lexicon.yaml              # NLP vocab
    schema/axes.yaml                     # age/sex/occupation axes
    examples/profiles/                   # p1, p2, p3
    PROSODY.md                           # pipeline docs
  docs/plan/                             # 9 plan pages
```

## Patterns the next chat must follow
- Terse, code-only unless asked. No verbose explanations.
- Ground-truth before spec: read the actual files via Filesystem MCP before assuming anything.
- Test before deliver: every script must run (or parse-check) before calling it done.
- `encoding="utf-8"` on EVERY Python IO site -- his machine is Windows (cp1252 default).
- `mkdir(parents=True, exist_ok=True)` on every output write -- no assumed dirs.
- All wording in blocks/drills is original; nothing verbatim from HMI/Panorama copyrighted workbooks.
- Port governance: check the census (123 ports) before claiming any new port.
- Secrets: never print env values in chat. The ElevenLabs key in tts_service.py is compromised.
- PSR sequence (`psr_full`) is Denise's exam order -- untouched, non-negotiable.
- Constraint C3: personas are fictional archetypes only, never real people.
- PHS anatomy everywhere: cue words + "for the purpose of hypnosis" + "with your permission" +
  quickly/calmly/deeply + body relaxes. After EVERY conversion and deepener in a first session.

## Immediate next steps
1. Test orchestrator + persona E2E on his Windows box:
   ```powershell
   # Terminal 1: persona-svc
   cd services\persona-svc
   $env:PERSONA_DIR="personas"; $env:OLLAMA_URL="http://localhost:11434"; $env:PORT="8601"
   python main.py
   # Terminal 2: orchestrator
   cd services\academy-orchestrator
   $env:SESSION_TEMPLATE_DIR="..\..\packages\session-templates"
   $env:PERSONA_URL="http://localhost:8601"; $env:PORT="8600"
   python main.py
   # Terminal 3: test
   curl http://localhost:8600/health
   curl -X POST http://localhost:8600/sessions -H "Content-Type: application/json" -d "{\"profile\":\"p1\",\"plan\":\"vocational\",\"persona\":\"maya\"}"
   # Then build /studio page to drive the WS
   ```
2. Build `/studio` page (pick profile/plan/persona -> WS transcript -> spacebar advance).
3. `.\academy.ps1 db-init` then `docker compose -f services\academy-compose.yml --profile p0 up -d`.
4. Rotate ElevenLabs key, fill services\.env, verify `:8136/docs` route.
