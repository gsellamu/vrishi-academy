{
  "name": "vrishi-academy",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "npm --workspace academy-web run dev",
    "build": "npm --workspace academy-web run build",
    "start": "npm --workspace academy-web run start"
  }
}
